import Foundation

/// Represents an extension of a type to conform it to a protocol
struct ProtocolConformance {
    /// The name of the type to extend
    var typeName: String
    
    /// The protocol to conform the type to
    var protocolName: String
}
